{
  "entities": {
    "ClassData": {
      "title": "ClassData",
      "description": "Consolidated class cash book and dues data for real-time multi-device synchronization",
      "type": "object",
      "properties": {
        "classConfig": {
          "type": "object",
          "description": "Class configuration and PIN settings"
        },
        "students": {
          "type": "array",
          "description": "List of registered students"
        },
        "jimpitanRecords": {
          "type": "array",
          "description": "List of student dues records"
        },
        "transactions": {
          "type": "array",
          "description": "List of cash in and cash out transactions"
        },
        "savingsGoals": {
          "type": "array",
          "description": "List of class savings targets"
        },
        "lastBackupDate": {
          "type": "string",
          "description": "Last backup date"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of last modification"
        }
      },
      "required": ["classConfig", "students", "jimpitanRecords", "transactions"]
    },
    "VisitorPresence": {
      "title": "VisitorPresence",
      "description": "Real-time presence and activity tracking for parents/wali murid opening the PWA",
      "type": "object",
      "properties": {
        "visitorId": {
          "type": "string",
          "description": "Unique identifier of the visitor session or device"
        },
        "role": {
          "type": "string",
          "description": "User role: wali_murid or pengurus"
        },
        "device": {
          "type": "string",
          "description": "Device platform description (e.g. HP Android, iPhone, PC)"
        },
        "openedAt": {
          "type": "string",
          "description": "Timestamp when the app was opened"
        },
        "lastSeen": {
          "type": "string",
          "description": "Timestamp of the last heartbeat ping"
        },
        "isOnline": {
          "type": "boolean",
          "description": "Whether the session is currently active"
        },
        "dateKey": {
          "type": "string",
          "description": "Date string YYYY-MM-DD for daily visit counting"
        }
      },
      "required": ["visitorId", "role", "lastSeen"]
    }
  },
  "firestore": {
    "/classes/{classId}": {
      "schema": "ClassData",
      "description": "Collection of synchronized class cash book datasets"
    },
    "/classes/{classId}/presence/{visitorId}": {
      "schema": "VisitorPresence",
      "description": "Real-time presence session records for wali murid and viewers"
    }
  }
}
